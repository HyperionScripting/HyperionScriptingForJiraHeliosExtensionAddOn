//Function for GET method, can be deleted if not used
const doGet: HttpEndpointFunction = (context, runtime) => {
}

//Function for HEAD method, can be deleted if not used
const doHead: HttpEndpointFunction = (context, runtime) => {
}

//Function for POST method, can be deleted if not used
const doPost: HttpEndpointFunction = (context, runtime) => {
}

//Function for PUT method, can be deleted if not used
const doPut: HttpEndpointFunction = (context, runtime) => {
}

//Function for DELETE method, can be deleted if not used
const doDelete: HttpEndpointFunction = (context, runtime) => {
}

//Function for OPTIONS method, can be deleted if not used
const doOptions: HttpEndpointFunction = (context, runtime) => {
}

//Function for TRACE method, can be deleted if not used
const doTrace: HttpEndpointFunction = (context, runtime) => {
}

//Function for PATCH method, can be deleted if not used
const doPatch: HttpEndpointFunction = (context, runtime) => {
}

//Function to get the sample admin config (must be serialized using JSON.stringify), return null if no config is needed
const getSampleAdminConfig: GetSampleAdminConfigFunction = () => {
    return null;
}